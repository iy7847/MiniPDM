# 로그 파일
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# 런타임 데이터
pids
*.pid
*.seed
*.pid.lock

# 의존성 라이브러리 (가장 중요!)
node_modules/

# 빌드 결과물 (Electron & Vite)
dist/
dist-electron/
release/

# 환경 설정 (보안상 절대 올리면 안 됨)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# OS 관련 임시 파일
.DS_Store
Thumbs.db

# IDE 설정 (선택 사항)
.idea/
.vscode/